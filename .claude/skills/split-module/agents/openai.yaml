interface:
  display_name: "Split Module"
  short_description: "Split oversized modules without behavior drift"
  default_prompt: "Use $split-module to perform a behavior-preserving split and keep every source file under the cap."
