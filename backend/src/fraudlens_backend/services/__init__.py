"""Backend application services (plan §16). Stateless helpers that orchestrate the
fraudlens-core primitives for the API/ingestion layers — e.g. `phi_mask` masks a
transaction's identifiers and computes its feature hash before persistence.
"""
