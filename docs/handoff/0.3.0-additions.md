# Release 0.3.0 additions and continuation handoff

This note separates implemented/reproducible work from paid evidence that is still pending. It
contains no subscription, support-case, contact, public-IP, credential, or local-path identifiers.

## Implemented and verified locally

- Full-data DuckDB/Parquet pipeline with frozen source verification, live-feature parity,
  whole-account temporal folds, calibration-derived thresholds, resumable checkpoints, and
  hash-bound publication contracts.
- vLLM BF16/AWQ harness with deterministic release cases, warm-up/development/full profiles,
  checkpointing, NVIDIA telemetry, quality/cost gates, RunPod operator lifecycle, and honest
  mechanical headlines.
- Durable investigation workers using PostgreSQL leases, heartbeats, fencing, reaping, bounded
  retry, replayable events, and explicit pending/running/retrying/failed/drafting-blocked UI states.
- Portable Kubernetes manifests, HPA, kind harness, AKS overlay/Terraform/inert workflow, and
  measured kind evidence: API replicas 1 → 5 → 1 with 100/100 durable runs after worker deletion.
- Research UI for inference/autoscaling/training evidence; absent paid artifacts render explicit
  pending states. Alert review shows risk/SHAP evidence and the backend-projected model input.
- Scalar at `/scalar`, OpenAPI JSON/YAML, standalone generated API HTML, nine indexed release ADRs,
  runbooks, claim register, interview guide, and deterministic README evidence regions.

## Paid evidence still pending

1. **Phase 6 CPU aggregate:** the Azure VM is deallocated after pilots, but the final Medium run,
   report publication, settled ledger row, resource destroy, and clean verification remain pending.
   Supporting resources may still bill until teardown.
2. **STOP 3 provider report:** after Phase 6 artifacts/cases are ready, refresh live Azure A100/A10
   quota and pricing plus RunPod Secure Cloud RTX 4090 availability/pricing. RunPod is the default;
   Azure is opportunistic only when actual quota and the pilot projection fit.
3. **Phase 11 GPU:** no RunPod or Azure GPU has been created and no BF16/AWQ compute has run. GPU
   creation requires the STOP 3 report, explicit provider selection, and separate paid approval.

## Safe continuation order

1. Run provider-free gates and preserve the final commit budget.
2. Obtain explicit permission before restarting the Azure CPU VM or changing its storage firewall.
3. Finish/export/publish the full-data report; obtain explicit teardown permission; destroy and
   verify clean; reconcile the ledger.
4. Build and validate the frozen full benchmark cases (1,000 measured + 40 development + warm-ups +
   20 abstention fixtures) and run the 100-case application-path validation.
5. Refresh provider facts and present STOP 3. Do not create a GPU in the same authorization step.
6. After explicit selection/approval, run AWQ smoke, both 40-case development arms, cost admission,
   both full arms, application pass, publication, stop, explicit destroy, and clean verification.

## Release-claim boundary

- Demonstrated now: local kind HPA/durability evidence.
- Tested/validate-only now: AKS Terraform, manifests, policy, and deployment workflow.
- Pending: full 68.2M-source-row aggregate and every BF16/AWQ result.
- Azure Container Apps remains the application deployment target; AKS is a demonstration runtime;
  RunPod is a temporary benchmark host only.
